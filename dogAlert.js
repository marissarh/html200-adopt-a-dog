/*Link JavaScript to your site.
Create click handlers that alerts the dog’s information 
when clicking on the dog’s photo.
You will need to pass in the params in the click handler 
of the dog’s name, breed and adoption fees.
Create click handlers on each dog’s button that will add 
to the total.
Alert this total.
This functionality and JavaScript should be present on 
both the index.html page and the dogs.html page (anywhere 
the dog card with adopt button is present).*/


//console.log("Is it working?");


function dogAlert(img){
    let dogName = h3;
    let dogFee = h4;
    let breed = p;
    let result= img;
    
    
    result = dogName + breed + dogFee;
}

alert(result); 